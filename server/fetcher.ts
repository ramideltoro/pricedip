import dns from 'node:dns/promises';import net from 'node:net';import {Agent,request} from 'undici';
export function publicIP(ip:string){if(net.isIP(ip)===4){const [a,b]=ip.split('.').map(Number);return !(a===0||a===10||a===127||a===169&&b===254||a===172&&b>=16&&b<=31||a===192&&b===168||a===100&&b>=64&&b<=127||a>=224||a===198&&(b===18||b===19));}return net.isIP(ip)===6&&/^2[0-9a-f]{3}:/i.test(ip)&&!ip.toLowerCase().startsWith('2001:db8:');}
export async function safeFetch(input:string,redirects=0):Promise<string>{const u=new URL(input);if(u.protocol!=='https:'||u.username||u.password||u.port&&u.port!=='443'||redirects>3)throw Error('Unsafe source URL');const hosts=await dns.lookup(u.hostname,{all:true});if(!hosts.length||hosts.some(h=>!publicIP(h.address)))throw Error('Source address is not public');const selected=hosts[0];const agent=new Agent({connect:{lookup:((_:any,_o:any,cb:any)=>cb(null,[{address:selected.address,family:selected.family}])) as any}});try{const r=await request(u,{dispatcher:agent,signal:AbortSignal.timeout(15000),headers:{'user-agent':'PriceDip/1.0 (+https://pricedip.ramideltoro.com)','accept':'text/html,application/json'}});if(r.statusCode>=300&&r.statusCode<400){const loc=r.headers.location;await r.body.dump();if(!loc||Array.isArray(loc))throw Error('Invalid redirect');return safeFetch(new URL(loc,u).href,redirects+1);}if(r.statusCode!==200){await r.body.dump();throw Error('Source unavailable (HTTP '+r.statusCode+')');}let raw='';let size=0;for await(const chunk of r.body){size+=chunk.length;if(size>2000000){r.body.destroy();throw Error('Source too large');}raw+=chunk.toString();}return raw;}finally{await agent.close();}}
